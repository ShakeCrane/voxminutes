# Dependencies
node_modules/
.pnp/
.pnp.*

# Lock files (pnpm-lock.yaml is committed for reproducible installs)
package-lock.json
yarn.lock

# Build outputs
target/
dist/
build/
.next/
out/
*.exe

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
venv/
.env/
.env
*.env
.venv-onnx/

# Models (large files) — 仅根目录的模型目录，勿误伤前端同名目录
/models/
/models-beifen/
*.onnx
*.bin
*.tar.bz2
*.zip
*.tar.gz

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Database — anchor to the repo root: an unanchored "database/" also swallowed
# frontend/src-tauri/src/database/, which is application source (same trap as /models/ above).
/database/
*.db
*.db-journal
*.db-shm
*.db-wal

# Audio
*.wav
# ...but the bundled ASR example clip is a declared Tauri resource
# (tauri.conf.json "example_audio.wav") and is read by the X-ASR benchmark test
# (sherpa_onnx_engine/engine.rs via CARGO_MANIFEST_DIR/../../example_audio.wav).
!frontend/src-tauri/example_audio.wav
!/example_audio.wav
*.mp3
*.m4a
*.flac
*.ogg

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Misc
*.tmp
*.temp
*.cache

# Local Rust toolchain config (contains machine-specific absolute paths / registry mirror)
.cargo/

# Unused icon set copied from reference project (app uses lucide-react; not referenced anywhere)
frontend/public/icons/

# Generated benchmark outputs (regenerate via scripts/benchmark_translation_models.py)
scripts/translation_benchmark_results.json
scripts/translation_benchmark_results.md

# 打包用运行时 DLL（从 target/debug 复制，体积大且随构建变化）
frontend/src-tauri/resources-dll/

# 本地开发工具（LLVM/libclang 等，勿提交）
.tooling/

# 内部交接文档（含项目来历与本机路径，不公开）
HANDOVER.md

# MVP 运行时已不使用的遗留 Python 后端（源自内部项目，暂不上传）
backend/

# 本地上传临时目录
.uploads/

# 旧架构（Python 后端时代）打包/工具脚本，已过时；新打包脚本后续重写
scripts/add-python-runtime.ps1
scripts/assemble-portable-package.ps1
scripts/benchmark_translation_models.py

# 开发环境工具产物（kilocode 插件声明，与产品无关）
/package.json
/pnpm-lock.yaml

# 内部测试/更新工具（MVP 不启用 updater）
scripts/inject_transcript.py
scripts/generate-update-manifest-github.js
scripts/test-update-locally.js

# 本地视频处理/推广工具产物（ffmpeg、剪映安装器、中间帧等，勿提交）
.tools/

# 本地工作记忆（WorkBuddy 项目记忆，不公开）
.workbuddy/

# 另一 AI 生成的推广素材目录（含 13MB 原始录屏，如需公开再单独提交）
docs/promotion/
